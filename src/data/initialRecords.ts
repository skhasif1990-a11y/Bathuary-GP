import { BeneficiaryRow } from '../types';

/**
 * INITIAL_BENEFICIARIES
 * Cleaned per requirement: No synthetic test/mock records.
 * The system strictly displays real data fetched and synchronized from the user's Google Sheet or uploaded master Excel.
 */
export const INITIAL_BENEFICIARIES: BeneficiaryRow[] = [];
